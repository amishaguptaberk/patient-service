# patient-service
The system provides RESTful APIs to manage patient records, including Create, Read, Update, and Delete (CRUD) operations. Additionally, it incorporates a React-based User Interface (UI) for user interaction and appointment scheduling functionality. 
The design ensures scalability, security, and ease of integration.